% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_forest.R
\name{GWAS_forest}
\alias{GWAS_forest}
\title{Generate Instrument-level Forest Plots for Mendelian Randomization}
\usage{
GWAS_forest(
  MR_input_data,
  report_form,
  save_plot = FALSE,
  file_type = "jpeg",
  save_dir = ".",
  xlim_custom = NULL,
  dot_size = 2,
  axis_text_size = 10,
  axis_title_size = 12,
  digits = 2,
  plot_width = 8,
  plot_height = 6,
  label_text_size = 3
)
}
\arguments{
\item{MR_input_data}{Harmonised MR input data frame. Must contain Outcome, Exposure,
Instrument, beta_exposure, se_exposure, beta_outcome, and se_outcome columns.}

\item{report_form}{Character string or vector indicating the standard
output scale for each outcome (e.g., "Beta", "OR", "HR"). Defaults to "Beta".}

\item{save_plot}{Logical; whether to save the generated plots to disk.
Default is FALSE.}

\item{file_type}{Character string specifying the file extension for
saving (e.g., "jpeg", "png", "pdf"). Default is "jpeg".}

\item{save_dir}{Character string specifying the directory path where
plots should be exported. Default is the current directory (".").}

\item{xlim_custom}{Optional numeric vector of length 2 providing custom
limits for the x-axis. If NULL, limits are determined by the data.}

\item{dot_size}{Numeric value specifying the size of the points. Default is 2.}

\item{axis_text_size}{Numeric value specifying the font size for axis labels.
Default is 10.}

\item{axis_title_size}{Numeric value specifying the font size for axis titles.
Default is 12.}

\item{digits}{Integer specifying the number of decimal places for labels.
Default is 2.}

\item{plot_width}{Numeric value specifying the width of exported plot file.
Default is 8.}

\item{plot_height}{Numeric value specifying the height of exported plot file.
Default is 6.}

\item{label_text_size}{Numeric value specifying the size of estimate labels
(Beta/OR/HR and 95 percent CI) shown on the plot. Default is 3.}
}
\value{
A list of ggplot objects, named by the Outcome-Exposure pair.
}
\description{
Creates a forest plot for each instrument (SNP) within the MR analysis,
including a pooled IVW estimate at the top for comparison.
}
\examples{
\donttest{
data("fi_49item")
input1 <- harmonize_mr_data(df = fi_49item)

GWAS_forest(
  MR_input_data = input1,
  report_form = "Beta",
  save_plot = FALSE,
  file_type = "jpeg",
  save_dir = ".",
  xlim_custom = NULL,
  dot_size = 2,
  axis_text_size = 10,
  axis_title_size = 12,
  digits = 2,
  plot_width = 8,
  plot_height = 6,
  label_text_size = 3
)
}
}
